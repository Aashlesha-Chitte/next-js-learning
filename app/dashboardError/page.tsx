let session: string | null = null;
export default function DashboardError() {
  // let error: Error | null = null;
  // const handleReset = () => {
  //   session = 'hii';
  //   return session;
  // }
  // if (!session) {
  //   throw new Error('An Error Occurred');
  // }
  return (
    <>
      {/* <Errors error={error} reset={handleReset} /> */}
      <h1 className="center">This is Dashboard</h1>
    </>
  )
}
